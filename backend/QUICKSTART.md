# 🚀 Quick Start Guide - Hackathon Backend

## การติดตั้งและรัน Backend

### 1. ติดตั้ง Dependencies
```bash
cd backend
npm install
```

### 2. ตั้งค่า Environment Variables
ไฟล์ `.env` ถูกสร้างไว้แล้ว แต่คุณสามารถแก้ไขได้ตามต้องการ:

```env
PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/hackathon
CORS_ORIGIN=http://localhost:3000
```

**หมายเหตุ:** 
- ถ้าใช้ MongoDB Atlas ให้แก้ `MONGODB_URI` เป็น connection string ของคุณ
- ถ้าใช้ MongoDB ในเครื่อง ต้องแน่ใจว่า MongoDB service กำลังรันอยู่

### 3. เริ่มต้น MongoDB (ถ้าใช้ในเครื่อง)
**Windows:**
```bash
# ถ้าติดตั้งเป็น service จะรันอัตโนมัติ
# หรือรันด้วยคำสั่ง:
mongod
```

**Mac/Linux:**
```bash
# ใช้ brew (Mac)
brew services start mongodb-community

# หรือรันโดยตรง
mongod --config /usr/local/etc/mongod.conf
```

### 4. เพิ่มข้อมูลตัวอย่าง (Optional)
```bash
npm run seed
```

คำสั่งนี้จะสร้างผู้ป่วยตัวอย่าง 5 ราย พร้อมข้อมูลทางการแพทย์ครบถ้วน

### 5. รัน Server

**Development Mode (แนะนำ - รองรับ auto-reload):**
```bash
npm run dev
```

**Production Mode:**
```bash
npm start
```

Server จะรันที่ `http://localhost:5000`

### 6. ทดสอบ API

เปิดเบราว์เซอร์ไปที่ `http://localhost:5000` คุณจะเห็น:
```json
{
  "success": true,
  "message": "Hackathon API Server is running! 🚀",
  "timestamp": "2024-02-14T15:48:34.123Z",
  "endpoints": {
    "users": "/api/users",
    "patients": "/api/patients"
  }
}
```

## 📝 ทดสอบ API ด้วย Postman หรือ cURL

### ดูผู้ป่วยทั้งหมด
```bash
curl http://localhost:5000/api/patients
```

### ดูผู้ป่วยตาม AN
```bash
curl http://localhost:5000/api/patients/an/AN2024001001
```

### สร้างผู้ป่วยใหม่
```bash
curl -X POST http://localhost:5000/api/patients \
  -H "Content-Type: application/json" \
  -d '{
    "name": "นายทดสอบ ระบบ",
    "AN": "AN2024999999",
    "dob": "1990-01-01",
    "sex": "M",
    "dateadm": "2024-02-14",
    "age": 34,
    "pdx": "J18.9",
    "drg": "195"
  }'
```

### ดูสถิติผู้ป่วย
```bash
curl http://localhost:5000/api/patients/stats/summary
```

### ค้นหาผู้ป่วยด้วย Query Parameters
```bash
# ค้นหาด้วยชื่อ
curl "http://localhost:5000/api/patients?name=สมชาย"

# ค้นหาด้วย DRG
curl "http://localhost:5000/api/patients?drg=280"

# ค้นหาด้วย Principal Diagnosis
curl "http://localhost:5000/api/patients?pdx=I21.9"
```

## 🔧 คำสั่งที่มีประโยชน์

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `npm start` | รัน server แบบ production |
| `npm run dev` | รัน server แบบ development (auto-reload) |
| `npm run seed` | เพิ่มข้อมูลตัวอย่างลงฐานข้อมูล |

## 📚 เอกสารเพิ่มเติม

- [README.md](../README.md) - คู่มือหลัก
- [PATIENT_SCHEMA.md](../docs/PATIENT_SCHEMA.md) - รายละเอียด Patient Schema

## ⚠️ Troubleshooting

### ปัญหา: Cannot connect to MongoDB
**แก้ไข:**
1. ตรวจสอบว่า MongoDB service กำลังรันอยู่
2. ตรวจสอบ `MONGODB_URI` ใน `.env` ว่าถูกต้อง
3. ถ้าใช้ MongoDB Atlas ตรวจสอบ IP whitelist และ credentials

### ปัญหา: Port 5000 already in use
**แก้ไข:**
1. เปลี่ยน `PORT` ใน `.env` เป็นเลขอื่น เช่น 5001
2. หรือปิดโปรแกรมที่ใช้ port 5000 อยู่

### ปัญหา: Module not found
**แก้ไข:**
```bash
rm -rf node_modules package-lock.json
npm install
```

## 🎯 Next Steps

1. ✅ Backend พร้อมใช้งานแล้ว!
2. 📱 ต่อไปสามารถสร้าง Frontend เพื่อเชื่อมต่อกับ API นี้
3. 🔐 เพิ่ม Authentication/Authorization ถ้าต้องการ
4. 📊 เพิ่ม API endpoints อื่นๆ ตามความต้องการ

## 💡 Tips

- ใช้ Postman หรือ Thunder Client (VS Code Extension) เพื่อทดสอบ API
- ดู logs ใน terminal เพื่อ debug
- ใช้ MongoDB Compass เพื่อดูข้อมูลในฐานข้อมูล
- อ่าน `PATIENT_SCHEMA.md` เพื่อเข้าใจโครงสร้างข้อมูลผู้ป่วย
